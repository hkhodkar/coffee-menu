export interface menuItem {
    id?: string,
    name: string,
    description?: string,
    price: number,
    category?: string,
    isAvailable?: boolean,
    isPopular?: boolean,
    image?: string,
}
